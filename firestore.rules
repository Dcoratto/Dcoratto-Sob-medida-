rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Permissive rules for testing/requested pattern
    match /{document=**} {
      allow read, write, delete: if request.auth != null;
    }
  }
}
